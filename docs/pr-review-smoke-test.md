# PR review smoke test

This file exists only to trigger the djimitflo-native PR review webhook
(Phase 1 rule-based stub) end-to-end against a real PR. Safe to delete
once the review comment + check run are confirmed to have posted.

Retrigger after gh CLI + auth were added to production.

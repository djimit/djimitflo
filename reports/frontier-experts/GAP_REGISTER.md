# Gap register (§46, §61)

Status vocabulary: PROVEN (directly demonstrated), PARTIALLY_PROVEN, NOT_PROVEN (code exists, operational claim unverified), BLOCKED (external/runtime dependency). Last update 2026-09-14, branch `feat/frontier-expert-intelligence`.

| # | Gap | Status | Evidence / blocker | Unblock |
|---|---|---|---|---|
| G-01 | Live arXiv enrichment of the 1 094 Pacing identities (E2 live smoke, E4 seed run) | BLOCKED (runner active) | arXiv export API throttles this client hard: after ~25 min of silence one request succeeds (HTTP 200 in 18 s), the next within a minute gets 429 `Rate exceeded.`; the VPS egress saw the same on 2026-09-14. A detached resumable runner (`src/scripts/enrich-experts.ts`, batch 5, 60 s spacing, 15 min back-off on 429/timeouts, log `scratchpad/enrich.log.jsonl`) is working through the seed DB; identities are only marked attempted after a completed request, so throttled ones are retried. Offline path PROVEN (`expert-evidence-enrichment.test.ts`, 5 tests). Operators can also trigger `POST /swarms/expert/enrich` (≤ 5 per call) and `POST /swarms/expert/ingest/pacing`. | Let the runner finish (hours at the current throttle); then sample 30 enriched identities for G-04 and rerun the benchmark on live data (G-03). |
| G-02 | Council with a real model (perspectives, adversary) | BLOCKED | `createModelPerspectiveRunner` needs `FRONTIER_EXPERTS_RUNTIME` and the provider module `social-runtime-providers.ts`, which lives on PR 223 (not merged). Anthropic API monthly spend limit reached until 2026-10-01; LiteLLM aliases on the workstation are usable once the module is available. Scripted runner PROVEN end-to-end (`expert-e2e-scenario.test.ts`, `expert-council.test.ts`). | User merges PR 223; set `FRONTIER_EXPERTS_RUNTIME=openai-compatible:<alias>` against LiteLLM; rerun `src/scripts/expert-e2e-scenario.ts` with the model runner injected. |
| G-03 | Benchmark on live data (real identities, real evidence) | NOT_PROVEN | Benchmark PROVEN on the synthetic fixture (`EXPERT_RESOLUTION_EVALUATION.md`); no ACTIVE real expert exists yet because G-01/G-02 and the human approval step have not run. | After G-01, human APPROVED/ACTIVE decisions on a handful of experts, then rerun the benchmark with `fixture` pointing at the live DB. |
| G-04 | Real-world identity resolution accuracy (name collisions) | NOT_PROVEN | Heuristic (`identityConfidence`) fails closed below 0.8 and is unit-tested on synthetic collisions (`J. Smith`), but its error rate on the real signatory list is unmeasured. | Sample 30 enriched identities after G-01, label manually, report identity-resolution error in EXPERT_RESOLUTION_EVALUATION.md. |
| G-05 | Prompt-injection escape with a live model | PARTIALLY_PROVEN | Builder quotes external content inert and the validator rejects impersonation and unknown refs (`expert-perspective-security.test.ts`, 3 tests). No adversarial run against a real model yet (depends on G-02). | Add injection payloads to the evidence excerpts and run the council with a live model; assert zero tool calls / policy changes. |
| G-06 | Skills (SkillService/OKF), MCP tools, UI, evolution/deprecation (§34–§36, §54–§55) | PARTIALLY_PROVEN | Skills: 14 §34 skills installed via `installFrontierSkills` into the OKF skills dir, all pass `SkillService.validate`, procedures injected into council perspective prompts (`frontier-expert-skills.test.ts`); `reasoning` and `human_ai_interaction` have no dedicated skill yet. MCP: 4 read-only tools (`experts.ts`, 2 tests). HTTP: 5 governed routes (`expert-routes.test.ts`). Evolution (§54): re-enrichment yields a versioned `capability_delta`; inferred deltas on ACTIVE experts are not recommended until `reviewCapability` checked+approved by different actors; an upsert bug that reset approved capabilities to inferred was found by the test and fixed. Deprecation (§55): typed `deprecate()` → REVOKED/REJECTED with history kept (`expert-evolution.test.ts`, 2 tests; routes for review and deprecate). UI (§36): `/frontier-experts` page (registry, detail, resolver, governed actions, swarm runs; `FrontierExpertsPage.test.ts`) verified in Chrome against a seeded local server. No scheduler triggers re-enrichment yet (bounded `enrichBatch` is manual). | A cron-style caller for `enrichBatch` once arXiv is reachable (G-01); UI check against live data after G-01. |
| G-07 | Performance at scale (§51) | PARTIALLY_PROVEN | Resolver 0.65 ms mean / 1.7 ms p95 per query on 74 identities with 100 evidence rows; ingestion of 1 386 signatories 259 ms. Not measured with thousands of ACTIVE experts and tens of thousands of evidence rows. | Generate a 10k-evidence fixture and profile `resolve`; add indexes if p95 > 50 ms. |
| G-08 | Legacy `expert-swarm-orchestrator.test.ts` is `describe.skip` (network adapters) | PARTIALLY_PROVEN | Pre-existing skip. The verification dead end and its fix are covered by `expert-swarm-verification.test.ts` (3 tests, not skipped) and the frontier path by `expert-council.test.ts` / `expert-e2e-scenario.test.ts`. | Replace the wikipedia-backed legacy tests with an injected adapter registry so they run offline. |
| G-09 | VERIFIED_FOR_USE path | PROVEN as designed | The heuristic judge never returns VERIFIED_FOR_USE; only an external checker or human can. No such checker exists yet, so nothing can currently reach `promoted`. | Implement the human/checker promotion endpoint with I06 separation (checker ≠ approver) when the review UI is built (G-06). |
| G-10 | Production activation | BLOCKED | Feature flag `DJIMITFLO_FRONTIER_EXPERTS_ENABLED` defaults off; branch not merged; prod container runs main. | User merges the PR, sets the flag and runtime, deploys with `/srv/djimitflo/deploy-agent-commons.sh <sha> main`. |

# Citation research route proof — G303

The authenticated local HTTP fixture registers a trusted source, creates two citation-linked opposing claims, detects the contradiction twice without duplicating the durable record, generates a cited report, and projects aggregate statistics. Source trust filtering and report confidence remain consistent over SQLite. Focused research route coverage passes 2/2.

This proves bounded local citation/research semantics and contradiction idempotency only; it does not validate external source truth, legal advice or provider quality.

# G282 compliance audit route proof

Focused HTTP/SQLite fixture: malformed append is rejected; two audit entries persist through the append-only `audit_events` chain/view, filtered log returns both, cryptographic verification returns `valid: true`, NORA report generation persists a report, and status reflects the verified chain/report. No external provider or deployment was used.

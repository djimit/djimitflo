# Workspace regression — G216

Command: `npm test -- --run`

Result: **2,782 tests passed, 20 skipped, 0 failed** across agent-catalog (26), dashboard (151), MCP (41), ransomware (40), server (2,491), shared (3) and Telegram (30). The existing non-Git diagnostic from an isolated negative fixture remains non-failing.

This run follows the complete route/MCP proof and verifies workspace integration after the new MCP smoke coverage.

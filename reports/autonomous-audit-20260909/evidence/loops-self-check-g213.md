# `/loops` self-check — G213

Command: `npx vitest run src/__tests__/*loop*.test.ts --config vitest.config.mts`

Result: **23 test files passed; 291 tests passed; 1 explicit skip; 0 failures** (2026-09-11).

This is a local governed-loop regression over disposable HTTP/SQLite/runtime fixtures. Negative admission, approval, recovery and stop controls remain active. It does not certify an external provider, merge, deployment or unseen-task improvement.

Raw output: [loops-self-check-g213.log](loops-self-check-g213.log).

# G254 browser availability

The browser skill was loaded and the in-app browser runtime was initialized, but `agent.browsers.list()` returned no connected browser instances. No browser action was simulated or inferred. Existing authenticated-local browser artifacts remain valid historical evidence; current browser revalidation is unavailable in this environment.

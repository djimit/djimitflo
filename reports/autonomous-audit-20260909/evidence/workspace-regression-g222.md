# G222 integrated workspace regression

After the SEGML Level-4 auth-boundary repair, the root `npm test` passes **2,785 tests** with **20 skipped** and **0 failures** across agent-catalog, dashboard, MCP, ransomware, server, shared and Telegram workspaces.

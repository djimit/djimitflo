# G271 assurance truth recheck

`assurance:truth` remains fail-closed after the `/skills` route proof. Node,
dependency audit, contract inventory (585 routes / 363 direct / zero critical
unclassified / 56 MCP), integration probes and diff checks pass. OpenMythos
evidence remains `BLOCKED` and live deployment identity remains `FAIL`; no
external prerequisite was bypassed.

# Risk, canvas and authority route proof — G295

The focused HTTP/SQLite chain passes 1/1. It proves:

- authority ledger trace, stats and filtered event pagination from a seeded append-only event;
- command and task risk assessment with typed decision output;
- live canvas session creation, status/list projection, thinking/tool-call/tool-result/diff/progress streaming and completion state.

The fixture is local and deterministic. It does not imply provider execution or production WebSocket/browser parity.

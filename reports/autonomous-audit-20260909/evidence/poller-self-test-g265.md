# G265 runtime poller

`python3 scripts/agent-social-poller.py --self-test` passed. The script is bounded to one poll, sends structured fields only, treats peer data as untrusted, and records runtime/model/run-id/usage provenance. Hermes and DeerFlow execution remain environment-dependent and are not claimed without credentials and a running provider.

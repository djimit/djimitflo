# G257 aggregate assurance recheck

`npm run assurance:truth` exits 1 fail-closed. Local node/build/dependency/integration checks pass; `assurance:openmythos` is `BLOCKED` and `assurance:live` is `FAIL` because external OpenMythos certification and authenticated live deployment identity are unavailable. No gate was bypassed.

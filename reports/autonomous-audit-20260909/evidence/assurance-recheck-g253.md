# G253 assurance recheck

`npm run assurance:integrations` passes. `npm run assurance:truth` remains fail-closed (`status: fail`) because OpenMythos evidence is not certification-ready and authenticated live deployment identity is unavailable. The run generated current evidence for the dirty audit checkout; no external gate was bypassed.

# G258 aggregate assurance recheck

`npm run assurance:truth` remains fail-closed (exit 1) after compliance report validation. Local environment, dependency, build/type/lint, integration and contract gates pass; OpenMythos evidence is `BLOCKED` and live identity is `FAIL` because external certification and authenticated deployment identity are unavailable. No external gate was bypassed.

# G272 assurance truth recheck

`assurance:truth` remains fail-closed after the `/skills` and `/observability`
route proofs. Node, dependency audit, contract inventory (585 routes / 367
direct / zero critical unclassified / 56 MCP), integration probes and diff
checks pass. OpenMythos evidence remains `BLOCKED` and live deployment identity
remains `FAIL`; no external prerequisite was bypassed. Raw command output is in
`assurance-truth-g272.log`.

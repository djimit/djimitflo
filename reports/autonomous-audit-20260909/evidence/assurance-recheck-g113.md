# G113 assurance recheck

After the governance-feedback route repair, `npm run assurance:truth` remains fail-closed with the same external blockers: supported Node, dependency audit, contract inventory, integration probes and diff checks pass; OpenMythos evidence is `BLOCKED`; live deployment identity is `FAIL`. This is not a local build or test regression.

# G222 `/loops` self-check

After the SEGML Level-4 auth-boundary repair, the canonical governed-loop Vitest run passes **23 test files and 291 tests**, with **1 explicit skip** and **0 failures**. Loop routing, budgets, recovery, runtime stop, security checker, completion and nested-spawn controls remain green.

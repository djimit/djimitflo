# G281 cognitive evolution route proof

Focused HTTP/SQLite fixture: invalid episode admission returns 400; three durable episodes (two successes, one failure) produce recorded-outcome patterns, evolve an advisory `careful` strategy for goal type `audit`, and expose a measured 2/3 success rate plus meta-learning totals. Strategy extraction is observational and explicitly not causal promotion or automatic dispatcher application.

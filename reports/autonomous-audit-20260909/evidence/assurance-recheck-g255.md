# G255 assurance recheck

`npm run assurance:integrations` passes. `npm run assurance:truth` remains fail-closed with `status: fail`: OpenMythos evidence is not certification-ready and authenticated live deployment identity is unavailable. No external gate was bypassed and no deployment was attempted after the usage telemetry repair.

# G129 council and red-team pagination validation

Council-session and adversarial red-team history endpoints now reject malformed `limit` values before session or history reads. The focused HTTP regression passes; the obsolete council expectation was corrected to the explicit 400 contract. The full server suite passes **2451/20 skipped**, the full workspace passes **2738/20 skipped**, `/loops` passes **12/12**, the route inventory passes **581 source routes / 263 contract-tested; 608/608 marked-auth anonymous denials** (614 instantiated registrations), and build/type-check/lint are green. No external provider, deployment, merge or production mutation was performed.

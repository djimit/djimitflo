# G162 `/loops` self-check

`npx vitest run src/__tests__/*loop*.test.ts --reporter=dot` ran after the
authenticated Mission Control browser traversal.

- 23 test files passed
- 291 tests passed
- 1 explicit skip
- expected negative controls remain exercised (budget, runtime, approval,
  cancellation, checker and spawn-token denials)

No source change was needed after the browser proof. This is a regression
recheck, not evidence of external provider quality or production promotion.

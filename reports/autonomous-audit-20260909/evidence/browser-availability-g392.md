# Browser availability G392

The required in-app browser runtime was selected and queried, but no connected browser instances were available (`agent.browsers.list()` returned an empty list). No browser state, credentials, cookies or external UI was changed. Existing authenticated local SPA crawl and production login evidence remain the latest browser proofs.

# Integration review

Status: REVIEW_REQUIRED before merge/deployment. This is local engineering review, not independent human approval or full security certification.

| Dimension | Assessment | Concrete scope |
|---|---|---|
| Security | Improved; residual boundaries explicit | Caller-bound MCP sessions, reserved recovery metadata, independent approval decisions and durable token revocation have adversarial regressions. CLI-internal tools still bypass ToolBroker. |
| Correctness | G72–G79 integrated server build/type/lint and2429tests pass;20skipped,75/75scoped mutation; authenticated OpenMythos/WorldLab intake and persisted Astra dispatch tests | Real restart/index/approval/browser failures reproduced, not inferred from test counts. Unknown provider outcomes cannot be treated as successful cancellation or safe replay. Two intermittent test/toolchain faults remain recorded. Principal-bound token replay, promotion-safe retest intake and persisted runtime dispatch are covered; native CLI mediation and live provider quality remain outside scope. |
| Performance | No new infrastructure; bounded native inventory | Async native socket scan avoids blocking Express. Whole-index search and all-anchor reads remain in-memory result sets from SQLite; no scale benchmark performed. |
| Maintainability | Existing seams reused | Existing engine, approval/audit services, tables, API client and platform tools retained. No new runtime/control plane/dependency. |

## Remaining findings and decisions

- G68–G71: actual browser/HTTP/SQLite/restart proof is joined in [lifecycle-closure.md](evidence/lifecycle-closure.md); final integrated rerun after the browser-discovered audit timeline correction passes 2,656 tests with 20 skipped, plus build/type/lint. Approval rows and canonical audit are atomic under SQLite faults; post-commit notification failure does not falsely roll back the domain outcome. Current execution inputs/expiry and policy after capacity waiting have independent adversarial engine tests. Hashes bind task values/referenced IDs, not every referenced file or configuration snapshot. [Atomicity](evidence/approval-atomicity.md), [admission review](evidence/queue-admission-revalidation.md).
- Core-agent registration no longer replaces FK parents; observational heartbeat cannot activate paused/offline/pending agents. Retirement rejects live work, retains exact owned snapshots/audit and uses existing offline/retired metadata rather than unsupported schema status. No process-drain or reactivation protocol is claimed. [Lifecycle review](evidence/agent-lifecycle-continuation.md).
- Review/export materialization tracks current canonical rows, retaining identity and explicit missing evidence; unknown/pending display is not green completion. Independent review separates current recorded execution approvals from manual/history rows; browser QA additionally caught duplicate synthetic approvals projected backward to request time. Canonical events now win per stage; legacy fallback is explicitly synthetic and uses only actual recorded decision timestamps, never fabricated expiry. Recalculation cost grows with the task's full event/evidence history; no scale benchmark is claimed. [Summary review](evidence/evidence-summary-review.md).
- OpenMythos failed loads cannot publish partially validated caches, and consumed snapshot hashes—not later disk bytes—identify run provenance. Explicit service recreation is required to adopt changed data. Actual maturity/repeatability/held-out gaps remain blocked. [Cache review](evidence/openmythos-cache-integrity.md).
- G66–G67 independent review reproduced WS membership bypass, MCP signature-only browser-session acceptance, cross-tab subject/family substitution, organization switch lost during renewal and delayed-JSON token overwrite. Each shared boundary was repaired and regression-tested. Actual browser→HTTP→SQLite proves same-family expiry/two-tab/restart/logout; legacy sidless revocation and universal tenant isolation are not claimed. Assurance source hashing now covers >1MiB patches without truncation; unclassified refresh canary was made an explicit real POST so the static inventory can see its already executed semantics. No new dependencies/provider calls. See the latest VERIFICATION_REPORT checkpoint.
- `packages/server/src/execution/execution-engine.ts:190`, startup recovery: valid only under the existing single-server-per-database model. An empty process map does not prove a real child is dead. Unknown attempts retain the protected hold; no automatic clearance/replay endpoint is introduced.
- `packages/server/src/services/loop-worker-executor-service.ts:351`, previous-task admission: recovery-held tasks now block replacement work, including cached-result bypass; a focused regression protects completed-result reuse.
- `packages/server/src/services/audit-anchoring.ts:178`, retry scheduling: timers do not automatically resume after restart. Durable diagnostics and manual dead-letter retry are tested; HTTP acceptance is not remote immutability proof.
- `packages/server/src/services/repository-index-service.ts:167`, search: vector/hybrid names are compatibility aliases for lexical scoring. Actual embeddings and vector retrieval are not implemented.
- `packages/server/src/services/tool-broker.ts:106`, mediation boundary: service tests establish token/policy behavior, not universal enforcement over native CLI tools.

Positive evidence: independent review found the pending-manual-approval dispatch regression, which was then repaired; table analysis exposed three genuine durable-state failures; actual browser QA found crashes and offscreen controls that the green baseline did not detect. Full regression results are recorded in VERIFICATION_REPORT.md.

Independent final G36 review reran the original cancel/redispatch process probe and10 focused tests. No blocking defect remained in that examined chain: cancellation waits for child-close, capacity remains reserved, and stale cleanup cannot remove the replacement lease. This bounded review is not approval to merge or a repository-wide security certification.

## Controlled product-source repair review

The parent and a separate review agent inspected the actual v2 maker, checker and security-checker commands/final verdicts, immutable regression tests, compiler/registry callers and generated patch. No blocking defect was found in the seven-line diff: it scans the four omitted instruction fields without changing overlap semantics or activation admission. The parent applied the exact diff after this review;26catalog tests and2380full-workspace tests pass. Actual runtime evidence remains in `evidence/controlled-product-improvement-v2.json`.

Independent worktree/thread separation is proven, not independent human judgment: runtime reviewers used the same model/account and read the executed check logs without rerunning tests. Three approvals were programmatic disposable-fixture approvals. The patch improves a heuristic scan; it does not make arbitrary prompt injection impossible. Human merge/deployment approval remains REVIEW_REQUIRED.

The independent loop review also found and repaired false certification of partial makers, cancelled-run resurrection and a mismatched superseded security-review filter. Those findings had actual SQLite/HTTP reproductions and red→green tests; they were not dismissed because the ordinary loop tests were green.
